

function Plus (number, number) {
    let result = number + number;
    return result;
}

function Minus (number, number) {
    let result = number - number;
    return result;
}

function Multiply (number, number) {
    let result = number * number;
    return result;
}

function Divide (number, number) {
    let result = number / number;
    return result;
}

function Equal () {
    
}

function Result () {
    result.style.display='block';
    result.innerHTML = 'Résultat : ' + Equal();
}
